services:
  web:
    build: ./app
    container_name: django-web
    volumes:
      - ./data:/app/data
      - static_volume:/app/staticfiles
      - ./media:/app/media
      - uwsgi-socket:/tmp/uwsgi
    command: >
      sh -c "python manage.py collectstatic --noinput &&
             gunicorn storefront.wsgi:application --bind 0.0.0.0:8000"
    expose:
      - 8000
    env_file:
      - .env_docker
    restart: unless-stopped

  celery_beat:
    build: ./app
    container_name: django-celery_beat
    volumes:
      - ./data:/app/data
      - static_volume:/app/staticfiles
      - ./media:/app/media
    command: ["celery", "-A", "storefront", "beat", "--loglevel=info"]
    env_file:
      - .env_docker
    restart: unless-stopped
    depends_on:
      - redis

  celery_worker:
    container_name: django-celery_worker
    build: ./app
    volumes:
      - ./data:/app/data
      - static_volume:/app/staticfiles
      - ./media:/app/media
    command: ["celery", "-A", "storefront", "worker", "--loglevel=info"]
    env_file:
      - .env_docker
    restart: unless-stopped
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]

  nginx:
    image: nginx:alpine
    container_name: nginx
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/certs:/etc/nginx/certs
      - ./media:/media
      - ./docs:/docs
      - static_volume:/app/staticfiles
      - uwsgi-socket:/tmp/uwsgi
    restart: unless-stopped
    depends_on:
      - web

volumes:
  uwsgi-socket:
  redis-data:
  static_volume: